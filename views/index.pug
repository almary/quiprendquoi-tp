include ../partials/header.pug
h1 Qui prend quoi ?
h2 Créer un événement
form(method="post" action="/party")
  div
    label(for="author") Nom de la personne
    input(name="author" id="author" required minlength="3" placeholder="Jean Dupont")
    label(for="name") Nom de l'événement
    input(name="name" id="name" required minlength="3" placeholder="Nouvel an")
  button(type="submit") Créer l'événement